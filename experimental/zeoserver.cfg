[buildout]
extends =
    ../plips/plipbase.cfg

parts +=
    zeoserver

[zeoserver]
recipe = plone.recipe.zeoserver
zeo-address = 127.0.0.1:8100


[instance]
zeo-client = True
zeo-address = ${zeoserver:zeo-address}
shared-blob = on

