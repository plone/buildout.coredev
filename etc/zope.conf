${instance:default-template}

<zodb_db main>
    mount-point /
    cache-size 10000
    <filestorage>
      path ${instance:filestorage}/Data.fs
      blob-dir ${instance:blobstorage}
    </filestorage>
</zodb_db>
