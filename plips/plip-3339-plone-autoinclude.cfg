[buildout]
extends =
    plipbase.cfg
# auto-checkout += plone.autoinclude

# I wanted to add it to the test eggs, but you quickly run into errors
# when a test tries to load configure.zcml from entry points,
# without first loading all meta.zcml:
# zope.configuration.exceptions.ConfigurationError:
# ('Unknown directive', 'http://namespaces.zope.org/zope', 'adapter')
# Also, without a checkout, the needed test-packages directory is not found.
# test-eggs += plone.autoinclude

[sources]
plone.autoinclude = git ${remotes:plone}/plone.autoinclude.git pushurl=${remotes:plone_push}/plone.autoinclude.git branch=main
Products.CMFPlone = git ${remotes:plone}/Products.CMFPlone.git pushurl=${remotes:plone_push}/Products.CMFPlone.git branch=plip-3339-plone.autoinclude

[versions]
plone.autoinclude = 1.0.0a2
